services:
  - type: web
    name: zefame
    env: python
    buildCommand: pip install -r requirements.txt && npm install
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
